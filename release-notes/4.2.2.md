# Agents Sleep Preventer 4.2.2

- Restores the menu bar's simple `Zz` / `ON n` sleep-state indicator.
- Keeps attention alerts inside the project dashboard instead of replacing the sleep indicator.
- Aligns the legacy menu paths with the same status language.
