# Agents Sleep Preventer 4.2.1

- Replaces the long diagnostic menu with a compact, project-first agent dashboard.
- Shows each agent's project and Git branch, with duplicate sessions grouped together.
- Uses Hermes profile names so concurrent gateways remain distinguishable.
- Reconciles attention alerts with live Claude Code session state to remove stale “Needs you” entries.
- Keeps idle projects available without letting them dominate the menu.
